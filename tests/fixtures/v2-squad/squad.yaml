name: test-v2-squad
version: 2.0.0
description: "A v2 test squad with state and model routing"
slashPrefix: tv2
state:
  enabled: true
  storage: file
  resume: true
model_strategy:
  orchestrator: "claude-sonnet-4"
  workers: "gemini-flash"
  reviewers: "claude-sonnet-4"
  override: true
components:
  agents:
    - agent-beta.md
  tasks:
    - task-beta.md
  workflows:
    - v2-workflow.yaml
  schemas:
    - output.json
